An MKGoal is an expression that either succeedes or fails. Success is represented by an instance of MKExecutionResult, failure by nil.

Instance variables:
  - block, the code implementing the goal behaviour.